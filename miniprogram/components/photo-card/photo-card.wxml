<!-- 瀑布流图片卡片 -->
<view class="photo-card" bind:tap="handleTap" role="button" aria-label="查看图片详情">
  <view class="image-wrap" style="height: {{cardHeight}}rpx">
    <image
      wx:if="{{!imageError && photo.thumbnail_url}}"
      src="{{photo.thumbnail_url}}"
      mode="aspectFill"
      lazy-load
      class="card-image"
      binderror="handleImageError"
    />
    <view wx:else class="image-placeholder">
      <text>图片加载失败</text>
    </view>
    <view class="note-mark" wx:if="{{photo.note_count > 0}}">
      <t-icon name="edit-1" size="28rpx" />
      <text wx:if="{{photo.note_count >= 2}}" class="note-count">{{photo.note_count}}</text>
    </view>
  </view>
</view>
