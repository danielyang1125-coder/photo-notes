.page { min-height:100vh; background:#f3f3f3; }
.content { min-height:100vh; }
.empty { display:flex; flex-direction:column; align-items:center; justify-content:center; padding-top:40vh; }
.empty-text { color:#999; font-size:28rpx; }
.empty-hint { color:#ccc; font-size:24rpx; margin-top:8rpx; }
.list { background:#fff; }
.loadmore-hint { text-align: center; padding: 24rpx; font-size: 24rpx; color: #999; }
